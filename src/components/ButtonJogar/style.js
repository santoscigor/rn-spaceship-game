import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  
  buttonPlayGame: {
    justifyContent: "center",
    width: 80,
    height: 80,
    borderRadius: 50,
    borderColor: "#000",
    borderWidth: 3,
    alignItems: "center",
    backgroundColor: "#e55f3d",
  },
  textPlay: {
    color: "#fff",
    fontSize: 20,
  },
});

export default style;